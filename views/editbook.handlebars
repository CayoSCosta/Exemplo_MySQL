<h1>Edite seu livro!</h1>
<form action="/books/update" method="POST" class="form">
<input type="hidden" name="id" value="{{book.id}}">
<div>
  <div class="form-control">
  <label for="tiyle">Título:</label>
  <input type="text" name="title" placeholder="Digite o título do livro." value="{{book.title}}"/>
  </div>

  <div class="form-control">
  <label for="pagesqty">Quantidade de páginas:</label>
  <input type="number" name="pagesqty" placeholder="Quantas páginas o livro tem?" value="{{book.pageqty}}"/>
  </div>
  
</div>
<input type="submit" value="Editar">
</form>